---
phase: 04-ui-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Shared/Extensions/Theme.swift
  - AFOne/Features/Dashboard/DashboardViewModel.swift
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "App automatically adapts colors to light/dark theme"
    - "Medical colors (AF=red, normal=green) stay constant regardless of theme"
    - "Dashboard can determine when data is empty vs loading"
  artifacts:
    - path: "AFOne/Shared/Extensions/Theme.swift"
      provides: "Centralized theme colors"
      min_lines: 50
    - path: "AFOne/Features/Dashboard/DashboardViewModel.swift"
      provides: "dataEmpty state for UI"
      contains: "dataEmpty"
  key_links:
    - from: "DashboardView"
      to: "Theme"
      via: "import"
      pattern: "Color\\.cardBackground"
---

<objective>
Create Theme.swift foundation for adaptive color system and update ViewModels with empty state tracking.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ui-enhancements/04-CONTEXT.md
@.planning/phases/04-ui-enhancements/04-RESEARCH.md

# Existing patterns from prior phases:

From Phase 1-3, the project uses:
- SwiftUI with @Observable macro
- MVVM architecture
- Color(.systemBackground) already in some views (adapts to theme)
- HealthKitService for data fetching

# Theme.swift must provide:

Global colors (adapt to theme via system colors):
- cardBackground: Color(.systemBackground)
- secondaryText: Color(.secondaryLabel)
- secondaryBackground: Color(.secondarySystemBackground)
- divider: Color(.separator)
- cardShadow: Color.black.opacity(0.1)

Semantic colors (CONSTANT - medical meaning):
- AFStatusColor: normal=green, af=red, unknown=gray
- HRColor: normal=green, elevated=orange, high=red
- BurdenColor: low=green, moderate=yellow, high=orange, veryHigh=red
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Theme.swift with color extensions</name>
  <files>AFOne/Shared/Extensions/Theme.swift</files>
  <action>
Create AFOne/Shared/Extensions/Theme.swift with:
1. Color extension with static properties for adaptive colors (cardBackground, secondaryText, secondaryBackground, divider, cardShadow)
2. AFStatusColor enum with static let for normal (green), af (red), unknown (gray) - these stay CONSTANT
3. HRColor enum with static let for normal, elevated, high - these stay CONSTANT
4. BurdenColor enum with static let for burden thresholds

The file should be placed in Shared/Extensions/ directory. Create the directory if it doesn't exist.
  </action>
  <verify>
<automated>ls AFOne/Shared/Extensions/Theme.swift && grep -q "cardBackground" AFOne/Shared/Extensions/Theme.swift</automated>
  </verify>
  <done>Theme.swift exists with Color extension and semantic color enums</done>
</task>

<task type="auto">
  <name>task 2: Add dataEmpty to DashboardViewModel</name>
  <files>AFOne/Features/Dashboard/DashboardViewModel.swift</files>
  <action>
Update DashboardViewModel to add a dataEmpty computed property:

```swift
var dataEmpty: Bool {
    recentEpisodes.isEmpty && episodeCount == 0 && averageHR == 0
}
```

This should be calculated after data is loaded (when isLoading becomes false).
  </action>
  <verify>
<automated>grep -q "dataEmpty" AFOne/Features/Dashboard/DashboardViewModel.swift</automated>
  </verify>
  <done>DashboardViewModel has dataEmpty computed property</done>
</task>

</tasks>

<verification>
- Theme.swift created with all required color definitions
- DashboardViewModel updated with dataEmpty property
- File compiles without errors
</verification>

<success_criteria>
Theme system is in place for subsequent UI updates. ViewModels can detect empty data state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhancements/04-01-SUMMARY.md`
</output>
