---
phase: 04-ui-enhancements
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - AFOne/Features/Timeline/TimelineView.swift
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Timeline shows loading indicator while data loads"
    - "Timeline shows helpful empty state when no rhythm data available"
    - "Empty state provides guidance to user"
  artifacts:
    - path: "AFOne/Features/Timeline/TimelineView.swift"
      provides: "Timeline with empty states"
      contains: "emptyStateView"
      contains: "isLoading"
  key_links: []
---

<objective>
Add empty states and loading indicators to TimelineView.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ui-enhancements/04-CONTEXT.md

# Current TimelineView:
- Has isLoading flag in ViewModel
- Shows horizontal scroll of DayBlockViews
- No explicit empty state when days array is empty

# Required changes:
1. Add loading state UI using ProgressView
2. Add empty state when no days data available
3. Empty message: "No rhythm data available yet"
</context>

<tasks>

<task type="auto">
  <name>task 1: Add empty state to TimelineView</name>
  <files>AFOne/Features/Timeline/TimelineView.swift</files>
  <action>
Update TimelineView to handle empty data state:

1. In the body, after the Picker, wrap the content:
```swift
if viewModel.isLoading {
    loadingView
} else if viewModel.days.isEmpty {
    emptyStateView
} else {
    // existing content
}
```

2. Add computed properties for loading and empty states:
```swift
private var loadingView: some View {
    VStack(spacing: 16) {
        ProgressView()
            .scaleEffect(1.5)
        Text("Loading timeline...")
            .font(.headline)
            .foregroundStyle(.secondary)
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
    .padding(.top, 100)
}

private var emptyStateView: some View {
    VStack(spacing: 16) {
        Image(systemName: "calendar.badge.exclamationmark")
            .font(.system(size: 60))
            .foregroundStyle(.secondary)
        
        Text("No Rhythm Data Available")
            .font(.headline)
        
        Text("Keep your Apple Watch on to monitor your heart rhythm over time.")
            .font(.subheadline)
            .foregroundStyle(.secondary)
            .multilineTextAlignment(.center)
            .padding(.horizontal, 40)
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
    .padding(.top, 100)
}
```

3. Ensure isLoading is set properly in loadData() method (already exists)
</action>
<verify>
<automated>grep -q "emptyStateView" AFOne/Features/Timeline/TimelineView.swift && grep -q "loadingView" AFOne/Features/Timeline/TimelineView.swift</automated>
  </verify>
  <done>TimelineView shows appropriate states for loading and empty data</done>
</task>

</tasks>

<verification>
- TimelineView shows loading indicator when isLoading is true
- TimelineView shows empty state when days array is empty
- Empty state provides user guidance
</verification>

<success_criteria>
TimelineView properly handles loading and empty data states
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-enhancements/04-03-SUMMARY.md`
</output>
