---
phase: 06-polish-localization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jdmartinez/Documents/Developer/af-one/AFOne/Localization/Localizable.xcstrings
  - /Users/jdmartinez/Documents/Developer/af-one/AFOne/App/ContentView.swift
  - /Users/jdmartinez/Documents/Developer/af-one/AFOne/Shared/Extensions/DateFormatter+Localization.swift
autonomous: true
requirements:
  - UI-05
---

<objective>
Establish localization foundation by creating a Localizable.xcstrings catalog, begin migrating user-facing strings to localized variants, and prepare locale-aware formatting (DateFormatter/NumberFormatter) and RTL layout considerations across SwiftUI views.
</objective>

<context>
@.planning/phases/06-polish-localization/06-RESEARCH.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@/Users/jdmartinez/Documents/Developer/af-one/AFOne/App/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Localizable.xcstrings catalog</name>
  <files>/Users/jdmartinez/Documents/Developer/af-one/AFOne/Localization/Localizable.xcstrings</files>
  <action>
Create a new Localizable.xcstrings string catalog:
- Create directory AFOne/Localization/ if needed
- Create Localizable.xcstrings file with JSON structure
- Add base language (English) as source language
- Add placeholder keys for all user-facing strings

Initial keys to add:
- Tab labels: "Dashboard", "Timeline", "Episodes", "Medications", "Analysis", "Trends", "More"
- System messages: "Checking HealthKit..."
- Common UI elements: "Close", "Back", "Save", "Cancel", "Error", "Loading..."
  </action>
  <verify>
File exists and contains at least Base language keys with common keys like tab_dashboard, tab_timeline, etc.
  </verify>
  <done>
Localizable.xcstrings created with base English translations
</done>
</task>

<task type="auto">
  <name>task 2: Aggregate user-facing strings from SwiftUI views</name>
  <files>/Users/jdmartinez/Documents/Developer/af-one/AFOne/App/ContentView.swift</files>
  <action>
Survey all user-facing strings in SwiftUI views under AFOne/Features and AFOne/App:
- Read each Swift file and identify Text(), Label(), Button titles
- Create corresponding keys in Localizable.xcstrings
- Prioritize ContentView.swift tab labels
- Add placeholder translations for each key

Key patterns to use:
- tab_dashboard, tab_timeline, tab_episodes, tab_medications, tab_analysis, tab_trends, tab_more
- system_checking_healthkit, system_loading, system_error
  </action>
  <verify>
All strings in ContentView.swift have corresponding keys in Localizable.xcstrings or are slated for replacement.
  </verify>
  <done>
All user-facing strings cataloged in Localizable.xcstrings
</done>
</task>

<task type="auto">
  <name>task 3: Replace literals with localized strings</name>
  <files>/Users/jdmartinez/Documents/Developer/af-one/AFOne/App/ContentView.swift</files>
  <action>
Refactor Text/Label content to use localized strings:
- For static strings: Use String(localized: "key") or LocalizedStringKey
- For dynamic strings: Use String(localized: String.LocalizationValue(key:))
- Ensure RTL semantics with .leading/.trailing where appropriate
- Update ContentView.swift tab labels to use localized keys

Example transformations:
- `Text("Dashboard")` → `Text(String(localized: "tab_dashboard"))`
- `Label("Timeline", systemImage: "chart.bar.xaxis")` → `Label(String(localized: "tab_timeline"), systemImage: "chart.bar.xaxis")`
  </action>
  <verify>
Localized usage present; no hard-coded strings remain for visible UI; tests for RTL alignment pass.
  </verify>
  <done>
ContentView.swift uses localized strings for all tab labels
</done>
</task>

<task type="auto">
  <name>task 4: Locale-aware formatting</name>
  <files>/Users/jdmartinez/Documents/Developer/af-one/AFOne/Shared/Extensions/DateFormatter+Localization.swift</files>
  <action>
Create or update DateFormatter and NumberFormatter utilities:
- Create DateFormatter+Localization.swift extension
- Add static properties for common formats (date, time, dateTime, relative)
- Use Locale.current to respect device language
- Add NumberFormatter for percentage, decimal, currency formats

Example patterns:
```swift
extension DateFormatter {
    static let localizedMedium: DateFormatter = {
        let formatter = DateFormatter()
        formatter.dateStyle = .medium
        formatter.timeStyle = .none
        formatter.locale = Locale.current
        return formatter
    }()
}
```
  </action>
  <verify>
Utilities exist and return localized date/number formats based on Locale; sample outputs align with device language.
  </verify>
  <done>
Locale-aware DateFormatter and NumberFormatter utilities available
</done>
</task>

<task type="auto">
  <name>task 5: RTL layout validation</name>
  <files>/Users/jdmartinez/Documents/Developer/af-one/AFOne/App/ContentView.swift</files>
  <action>
Test right-to-left (RTL) layouts:
- Use semantic layouts (.leading/.trailing instead of .left/.right)
- Ensure icons maintain correct orientation
- Test in simulator with Arabic (ar) or Hebrew (he) locale
- Verify card layouts, navigation bars, and bottom bar work correctly

SwiftUI handles most RTL automatically, but verify:
- Leading/trailing alignment is correct
- Tab bar icons and text flow correctly
- Card content aligns properly
  </action>
  <verify>
RTL rendering works without overlap; all views respect semantic directions.
  </verify>
  <done>
RTL layout validated - app ready for Arabic/Hebrew locales
</done>
</task>

</tasks>

<verification>
- Localizable.xcstrings file exists after plan execution
- ContentView.swift updated to reference localized strings
- Date/Number formatting utilities created and tested
- RTL layouts render correctly across test locales
</verification>

<success_criteria>
- App displays in device language
- All user-facing strings sourced from catalog
- Locale-aware formatting works
- RTL support validated
- UI-04 remains unaffected by localization changes
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-localization/06-02-SUMMARY.md`
</output>