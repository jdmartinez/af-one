---
phase: 08-new-color-palette
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Shared/Extensions/Theme.swift
  - AFOne/Features/Timeline/TimelineView.swift
autonomous: true
gap_closure: true
requirements: [UI-01, UI-02]
---

<objective>
Fix critical build errors and Color Set reference issues identified in VERIFICATION.md

Purpose: Close gaps that prevent Phase 08 from achieving its goal
Output: Working color palette implementation with correct Color Set references
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-new-color-palette/08-VERIFICATION.md
@.planning/phases/08-new-color-palette/08-05-SUMMARY.md
@AFOne/Shared/Extensions/Theme.swift
@AFOne/Features/Timeline/TimelineView.swift
</context>

<tasks>

<task type="auto">
  <name>task 1: Fix Theme.swift Color Set references</name>
  <files>AFOne/Shared/Extensions/Theme.swift</files>
  <action>
    Fix Color Set references in Theme.swift (lines 28-32). The current references use incorrect path format:
    
    CURRENT (WRONG):
    - Color("AFOne/AFOneRhythmSinusal")
    - Color("AFOne/AFOneRhythmAF")
    - Color("AFOne/AFOneBurdenLow")
    - Color("AFOne/AFOneBurdenMid")
    - Color("AFOne/AFOneBurdenHigh")
    
    CORRECT (use root-level Color Sets):
    - Color("AFOneRhythmSinusal")
    - Color("AFOneRhythmAF")
    - Color("AFOneBurdenLow")
    - Color("AFOneBurdenMid")
    - Color("AFOneBurdenHigh")
    
    These root-level Color Sets exist in Assets.xcassets/ directory.
  </action>
  <verify>
    <automated>xcodebuild -project AFOne.xcodeproj -scheme AFOne -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "(error:|warning:.*Color)" | head -20</automated>
  </verify>
  <done>Theme.swift references correct Color Set paths - colors will load at runtime</done>
</task>

<task type="auto">
  <name>task 2: Fix TimelineView.swift build errors</name>
  <files>AFOne/Features/Timeline/TimelineView.swift</files>
  <action>
    Fix two build errors where .systemGray is used without Color() wrapper:
    
    Line 262: Change `LegendItem(color: .systemGray, label: "Unknown")` 
              to `LegendItem(color: Color(.systemGray), label: "Unknown")`
    
    Line 283: Change `case .unknown: return .systemGray` 
              to `case .unknown: return Color(.systemGray)`
    
    Note: Lines 315 and 319 already correctly use Color(.systemGray) - leave those as-is.
  </action>
  <verify>
    <automated>xcodebuild -project AFOne.xcodeproj -scheme AFOne -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "error:" | head -10</automated>
  </verify>
  <done>TimelineView.swift compiles without errors</done>
</task>

<task type="auto">
  <name>task 3: Verify build succeeds</name>
  <files>AFOne/Shared/Extensions/Theme.swift, AFOne/Features/Timeline/TimelineView.swift</files>
  <action>
    Run full build to verify both fixes work together:
    
    xcodebuild -project AFOne.xcodeproj -scheme AFOne -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16' build
    
    The build must succeed with no errors.
  </action>
  <verify>
    <automated>xcodebuild -project AFOne.xcodeproj -scheme AFOne -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds - Phase 08 color palette implementation complete</done>
</task>

</tasks>

<verification>
After executing all tasks:
1. Theme.swift uses correct Color Set references (e.g., Color("AFOneRhythmSinusal"))
2. TimelineView.swift compiles without .systemGray errors
3. Build succeeds with no errors
</verification>

<success_criteria>
- [ ] Theme.swift Color Set references fixed to use correct paths
- [ ] TimelineView.swift build errors resolved
- [ ] Full project build succeeds
- [ ] All colors render correctly at runtime (verified via simulator if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-color-palette/08-06-SUMMARY.md`
</output>
