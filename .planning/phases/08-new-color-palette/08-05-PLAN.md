---
phase: "08"
plan: "05"
type: execute
wave: 4
depends_on:
  - "08-01"
files_modified:
  - "AFOne/Features/Overview/OverviewView.swift"
  - "AFOne/Features/Timeline/RhythmMapView.swift"
  - "AFOne/Features/Timeline/TimelineView.swift"
  - "AFOne/Features/Authorization/AuthorizationView.swift"
  - "AFOne/Shared/Components/TriggerChip.swift"
  - "AFOne/Features/Report/DisclaimerView.swift"
autonomous: true
requirements:
  - "UI-01"
  - "UI-02"
must_haves:
  truths:
    - "No hardcoded colors remain in view files"
    - "All colors use semantic or clinical color tokens"
  artifacts:
    - path: "Multiple view files"
      provides: "Color references using semantic tokens"
      min_files: 6
---

<objective>
Sweep all view files and replace hardcoded colors with proper semantic/clinical tokens. Per SPEC.md Section 6 - Prohibited color practices.

**Purpose:** Eliminates Color.red, Color.green, Color.blue and other hardcoded colors throughout the codebase.

**Output:** Updated view files with semantic color references
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-new-color-palette/08-CONTEXT.md
@.planning/phases/08-new-color-palette/SPECIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Replace hardcoded colors in OverviewView</name>
  <files>AFOne/Features/Overview/OverviewView.swift</files>
  <action>
    Replace hardcoded colors in OverviewView.swift:
    
    - `Color.red` → `Color.afOne.rhythmAF` (clinical AF indicator)
    - `Color.green` → `Color.afOne.rhythmSinusal` (clinical SR indicator)
    
    Use grep to find all hardcoded color usages first, then replace.
  </action>
  <verify>
    <automated>grep -c 'Color\.red\|Color\.green' AFOne/Features/Overview/OverviewView.swift 2>/dev/null || echo "0"</automated>
  </verify>
  <done>
    OverviewView.swift has no Color.red or Color.green
  </done>
</task>

<task type="auto">
  <name>task 2: Replace hardcoded colors in RhythmMapView</name>
  <files>AFOne/Features/Timeline/RhythmMapView.swift</files>
  <action>
    Replace hardcoded colors in RhythmMapView.swift:
    
    - `Color.blue` → `Color(.systemBlue)` (structural, not clinical)
    
    Check for other hardcoded colors like Color.gray, Color.orange.
  </action>
  <verify>
    <automated>grep -c 'Color\.blue' AFOne/Features/Timeline/RhythmMapView.swift 2>/dev/null || echo "0"</automated>
  </verify>
  <done>
    RhythmMapView.swift has no Color.blue
  </done>
</task>

<task type="auto">
  <name>task 3: Replace hardcoded colors in TimelineView</name>
  <files>AFOne/Features/Timeline/TimelineView.swift</files>
  <action>
    Replace hardcoded colors in TimelineView.swift:
    
    - `Color.red` → `Color.afOne.rhythmAF`
    - `Color.green` → `Color.afOne.rhythmSinusal`
    - Check for other hardcoded colors
  </action>
  <verify>
    <automated>grep -cE 'Color\.(red|green|blue|orange|gray)' AFOne/Features/Timeline/TimelineView.swift 2>/dev/null || echo "0"</automated>
  </verify>
  <done>
    TimelineView.swift has no hardcoded basic colors
  </done>
</task>

<task type="auto">
  <name>task 4: Replace hardcoded colors in AuthorizationView</name>
  <files>AFOne/Features/Authorization/AuthorizationView.swift</files>
  <action>
    Sweep AuthorizationView.swift for hardcoded colors:
    
    Replace with appropriate semantic colors:
    - Background colors → `Color(.systemBackground)` or `Color(.secondarySystemBackground)`
    - Text colors → Use text opacity modifiers or semantic colors
    - Accent colors → `Color(.systemBlue)` for interactive elements
  </action>
  <verify>
    <automated>grep -cE 'Color\(red:|Color\.red|Color\.green|Color\.blue' AFOne/Features/Authorization/AuthorizationView.swift 2>/dev/null || echo "0"</automated>
  </verify>
  <done>
    AuthorizationView.swift has no hardcoded colors
  </done>
</task>

<task type="auto">
  <name>task 5: Replace hardcoded colors in TriggerChip and DisclaimerView</name>
  <files>AFOne/Shared/Components/TriggerChip.swift, AFOne/Features/Report/DisclaimerView.swift</files>
  <action>
    Sweep TriggerChip.swift and DisclaimerView.swift for hardcoded colors:
    
    Replace with:
    - Structural colors → semantic colors (systemBackground, secondarySystemBackground)
    - Unknown state → `Color(.systemGray)`
    - Use clinical tokens where the color has clinical meaning
  </action>
  <verify>
    <automated>grep -cE 'Color\(red:|Color\.red|Color\.green|Color\.blue|Color\.gray|Color\.orange' AFOne/Shared/Components/TriggerChip.swift AFOne/Features/Report/DisclaimerView.swift 2>/dev/null || echo "0"</automated>
  </verify>
  <done>
    TriggerChip and DisclaimerView have no hardcoded colors
  </done>
</task>

</tasks>

<verification>
- [ ] No Color.red, Color.green, Color.blue in any view files
- [ ] All replaced colors use semantic or clinical tokens
- [ ] Code compiles without errors
</verification>

<success_criteria>
All hardcoded colors removed from view files, replaced with semantic/clinical tokens
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-color-palette/08-05-SUMMARY.md`
</output>
