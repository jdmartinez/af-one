---
phase: "08"
plan: "04"
type: execute
wave: 3
depends_on: []
files_modified:
  - "AFOne/Shared/Extensions/TextOpacity.swift"
autonomous: true
requirements:
  - "UI-01"
must_haves:
  truths:
    - "Text opacity modifiers available as ViewExtensions"
    - "All 6 opacity levels defined (primary, high, secondary, tertiary, quaternary, fill)"
  artifacts:
    - path: "AFOne/Shared/Extensions/TextOpacity.swift"
      provides: "Text opacity ViewModifiers"
      min_lines: 30
---

<objective>
Implement text opacity scale as SwiftUI ViewModifiers. Per SPEC.md Section 3.

**Purpose:** Provides consistent text opacity levels across the app without hardcoding opacity values.

**Output:** New TextOpacity.swift with View extensions and enum
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-new-color-palette/SPECIFICATION.md
</context>

<tasks>

<task type="auto">
<name>task 1: Create text opacity modifiers</name>
  <files>AFOne/Shared/Extensions/TextOpacity.swift</files>
  <action>
    Create TextOpacity.swift with:
    
    1. **TextOpacityLevel enum** - six cases matching SPEC.md:
       - primary: 100% (uses .primary)
       - high: 85%
       - secondary: 50% (uses .secondary)
       - tertiary: 30% (uses .tertiary)
       - quaternary: 18%
       - fill: 7%
    
    2. **View extension** with textOpacity(_:) modifier:
       ```swift
       extension View {
           func textOpacity(_ level: TextOpacityLevel) -> some View {
               self.foregroundStyle(.primary.opacity(level.opacity))
           }
       }
       ```
    
    Use .foregroundStyle() for iOS 15+ compatibility with semantic colors.
  </action>
  <verify>
    <automated>test -f AFOne/Shared/Extensions/TextOpacity.swift && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>
    TextOpacity.swift exists with all 6 opacity levels
  </done>
</task>

</tasks>

<verification>
- [ ] TextOpacity.swift created
- [ ] All 6 opacity levels defined
- [ ] View extension compiles
</verification>

<success_criteria>
Text opacity modifiers available for use in SwiftUI views
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-color-palette/08-04-SUMMARY.md`
</output>
