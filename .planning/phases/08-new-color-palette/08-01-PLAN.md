---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "AFOne/Theme.swift"
autonomous: true
requirements:
  - "UI-01"
  - "UI-02"
must_haves:
  truths:
    - "Theme.swift uses Color Set references instead of hardcoded RGB"
    - "All clinical colors reference Assets.xcassets"
  artifacts:
    - path: "AFOne/Theme.swift"
      provides: "Semantic color definitions using Color Set pattern"
      min_lines: 50
  key_links:
    - from: "AFOne/Theme.swift"
      to: "AFOne/Assets.xcassets"
      via: "Color(\"AFOne/Name\") references"
---

<objective>
Fix Theme.swift to use Color Set asset references instead of hardcoded RGB values. Establishes the foundational pattern that all other color implementations depend on.

**Purpose:** Implements the core theme structure per SPEC.md Section 1 - Clinical tokens must reference Assets.xcassets for appearance variant support.

**Output:** Updated Theme.swift with Color("AFOne/...") pattern
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-new-color-palette/08-CONTEXT.md
@.planning/phases/08-new-color-palette/SPECIFICATION.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Replace hardcoded RGB with Color Set references</name>
  <files>AFOne/Theme.swift</files>
  <action>
    Update Theme.swift to replace all `Color(red:green:blue:)` calls with Color Set references:
    
    Replace pattern:
    ```swift
    Color(red: 0.204, green: 0.780, blue: 0.600)  // #34d399
    ```
    With:
    ```swift
    Color("AFOne/RhythmSinusal")
    ```
    
    Required replacements:
    - RhythmSinusal: #34d399 dark / #059669 light / #00c87a high contrast
    - RhythmAF: #f87171 dark / #dc2626 light / #ff3b30 high contrast
    - BurdenLow: #34d399 dark / #059669 light / #00c87a high contrast  
    - BurdenMid: #fbbf24 dark / #d97706 light / #ffcc00 high contrast
    - BurdenHigh: #f87171 dark / #dc2626 light / #ff3b30 high contrast
    
    Structure: Create AFOneColors struct with let properties for each clinical color.
  </action>
  <verify>
    <automated>grep -c 'Color(red:' AFOne/Theme.swift || echo "0"</automated>
  </verify>
  <done>
    Theme.swift has 0 occurrences of Color(red:green:blue:)
    Theme.swift has Color("AFOne/...") references for all 5 clinical tokens
  </done>
</task>

</tasks>

<verification>
- [ ] Theme.swift has no hardcoded RGB values
- [ ] All clinical colors use Color Set pattern
- [ ] Code compiles without errors
</verification>

<success_criteria>
Theme.swift compiles and uses Color("AFOne/Name") pattern for all clinical colors
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-color-palette/08-01-SUMMARY.md`
</output>
