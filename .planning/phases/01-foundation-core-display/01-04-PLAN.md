---
phase: 01-foundation-core-display
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified: []
autonomous: true
requirements: [BURN-01, TIME-01]
must_haves:
  truths:
    - "Timeline shows rhythm status across time with color-coded daily blocks"
    - "AF burden displayed as percentage with color indicator"
  artifacts:
    - path: "AFOne/Features/Timeline/TimelineView.swift"
      provides: "24h timeline blocks with 7/30 day views"
    - path: "AFOne/Core/Analysis/AFBurdenCalculator.swift"
      provides: "AF burden percentage calculation"
  key_links:
    - from: "TimelineView"
      to: "HealthKitService.fetchEpisodes()"
      via: "ViewModel query"
    - from: "BurdenCard"
      to: "AFBurdenCalculator"
      via: "Percentage computation"
---

<objective>
Implement Timeline visualization with 24-hour blocks and AF Burden display.

Purpose: Users can visualize rhythm status across time and see AF burden percentage.
Output: Horizontal scrollable timeline with daily blocks, burden percentage card.
</objective>

<context>
@.planning/phases/01-foundation-core-display/01-02-PLAN.md
@.planning/phases/01-foundation-core-display/01-02-SUMMARY.md (will exist after plan 02)

## Key Context from 1-CONTEXT.md

**Timeline Decision:** Daily blocks (24h segments)
- Horizontal scrollable timeline
- Each day is one "block" showing dominant rhythm
- Color-coded: green=normal, red=AF, gray=unknown
- Tap block to see hourly breakdown
- Default view: last 7 days
- Filter options: 7 days, 30 days, custom range
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Timeline view with 24h blocks</name>
  <files>
    AFOne/Features/Timeline/TimelineView.swift
    AFOne/Features/Timeline/TimelineViewModel.swift
    AFOne/Shared/Components/RhythmBlockView.swift
  </files>
  <action>
    Create TimelineView:
    
    **Header:**
    - Segmented control: "7 Days" | "30 Days" | "Custom"
    - Default: 7 Days
    
    **Timeline (horizontal scroll):**
    - Each day is a vertical block (~60pt wide)
    - Shows date label (Mon, Tue, etc.)
    - Background color: green (normal day), red (AF day), gray (no data)
    - Dominant rhythm determines color (>50% of day in that state)
    
    **Detail View (on tap):**
    - Show 24-hour horizontal bar for selected day
    - Hourly segments showing rhythm at each hour
    - Legend: Green=Normal, Red=AF, Gray=Unknown
    
    Use Swift Charts for the hourly breakdown visualization.
    Query: fetchEpisodes(from: Date, to: Date) for the period.
  </action>
  <verify>
    <automated>MISSING — Build test after this task completes</automated>
  </verify>
  <done>
    Horizontal scrollable timeline with 7/30 day options.
    Daily blocks color-coded by dominant rhythm.
    Tap block shows hourly breakdown.
    Color legend displayed.
  </done>
</task>

<task type="auto">
  <name>task 2: Create AF Burden calculator and display</name>
  <files>
    AFOne/Core/Analysis/AFBurdenCalculator.swift
    AFOne/Shared/Components/BurdenCard.swift
  </files>
  <action>
    Create AFBurdenCalculator:
    
    func calculateBurden(from samples: [AFBurdenSample], period: BurdenPeriod) -> Double
    - period: .daily, .weekly, .monthly
    - Returns percentage (0.0 to 100.0)
    - Handle empty data: return nil
    
    Create BurdenCard component:
    - Large percentage display (e.g., "12.4%")
    - Period label ("Last 7 days")
    - Color indicator: green (<1%), yellow (1-5%), orange (5-20%), red (>20%)
    - "AF Burden" title
    
    Integrate into Dashboard and Timeline views.
  </action>
  <verify>
    <automated>MISSING — Build test after this task completes</automated>
  </verify>
  <done>
    AF burden calculated as percentage.
    BurdenCard displays with color-coded indicator.
    Available for daily, weekly, monthly periods.
  </done>
</task>

</tasks>

<verification>
- Timeline shows 7/30 day views
- Daily blocks color-coded (green/red/gray)
- Hourly breakdown on block tap
- AF burden shown as percentage with color indicator
</verification>

<success_criteria>
User can view AF burden as percentage. Timeline shows rhythm status across time with color-coded daily blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-display/01-04-SUMMARY.md`
</output>
