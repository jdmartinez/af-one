---
phase: 01-foundation-core-display
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: true
requirements: [FOUN-01, EMER-01, EMER-02]
must_haves:
  truths:
    - "Authorization requested on first launch before accessing any data"
    - "Emergency info read from HealthKit Medical ID"
  artifacts:
    - path: "AFOne/Features/Authorization/"
      provides: "Authorization flow with upfront permission request"
    - path: "AFOne/Features/More/EmergencyView.swift"
      provides: "Emergency info from Medical ID"
  key_links:
    - from: "AuthorizationView"
      to: "HealthKitService.requestAuthorization()"
      via: "ViewModel async call"
    - from: "EmergencyView"
      to: "HKHealthStore"
      via: "Medical ID read"
---

<objective>
Implement HealthKit authorization flow and Emergency information view.

Purpose: Authorization is required before any data access. Emergency info is quick-access from both Dashboard and More tab.
Output: User can authorize HealthKit access; emergency info readable from Medical ID.
</objective>

<context>
@.planning/phases/01-foundation-core-display/01-01-PLAN.md
@.planning/phases/01-foundation-core-display/01-01-SUMMARY.md (will exist after plan 01)

## Key Context from 1-CONTEXT.md

**Authorization Decision:** Upfront full request on first launch
- Show brief explanation screen first
- Request all required permissions in single prompt:
  - Heart rate, AF burden (iOS 16+), ECG readings, HRV
- After authorization, proceed to Dashboard
- Handle denied state with "Enable in Settings" guidance

**Emergency Decision:** Read from HealthKit Medical ID
- Read emergency contact, medical conditions, medications from HealthKit
- Display in Emergency tab with "From Medical ID" attribution
- No editing capability (direct to Health app)
- Additional free-text notes allowed (SwiftData - Phase 2)
</context>

<tasks>

<task type="auto">
  <name>task 1: Create authorization flow</name>
  <files>
    AFOne/Features/Authorization/AuthorizationView.swift
    AFOne/Features/Authorization/AuthorizationViewModel.swift
  </files>
  <action>
    Create AuthorizationView shown on first launch:
    1. Brief explanation screen: "AFOne reads heart rhythm data from Apple Watch"
    2. "Continue" button triggers requestAuthorization() from HealthKitService
    3. Request all types: heartRate, atrialFibrillationBurden, heartRateVariability, electrocardiogram
    4. On success: navigate to Dashboard
    5. On denied: show clear guidance "Enable in Settings → Privacy → Health"
    
    Use @AppStorage("hasCompletedOnboarding") to track completion.
    Handle isHealthDataAvailable() check first.
  </action>
  <verify>
    <automated>MISSING — Build test after this task completes</automated>
  </verify>
  <done>
    First launch shows explanation before permission request.
    Single prompt requests all required HealthKit permissions.
    Denied state shows Settings guidance.
  </done>
</task>

<task type="auto">
  <name>task 2: Create Emergency view</name>
  <files>
    AFOne/Features/More/EmergencyView.swift
    AFOne/Features/More/EmergencyViewModel.swift
  </files>
  <action>
    Create EmergencyView:
    - Read from HealthKit Medical ID using HKHealthStore's emergencyContacts, medicalConditions, medications
    - Display sections:
      - Diagnosis: "Atrial Fibrillation" (hard-coded for now)
      - Current Medications (from Medical ID)
      - Emergency Contact (from Medical ID)
      - "From Medical ID" attribution below each section
    - "Open Health App" button for editing Medical ID
    
    Add Emergency quick action to Dashboard (top-right button).
    Add to More tab navigation.
  </action>
  <verify>
    <automated>MISSING — Build test after this task completes</automated>
  </verify>
  <done>
    Emergency info displays from Medical ID.
    Attribution shows "From Medical ID".
    Quick access from Dashboard and More tab.
  </done>
</task>

</tasks>

<verification>
- AuthorizationView shown before Dashboard on first launch
- HealthKit permissions requested for all required types
- EmergencyView reads from Medical ID (not custom storage)
</verification>

<success_criteria>
User can complete HealthKit authorization flow. Emergency information accessible from Dashboard and More tab.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-display/01-02-SUMMARY.md`
</output>
