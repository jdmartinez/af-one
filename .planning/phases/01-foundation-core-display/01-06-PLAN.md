---
phase: 01-foundation-core-display
plan: 06
type: execute
wave: 3
depends_on: [01-02]
files_modified: []
autonomous: true
requirements: [NOTIF-01]
must_haves:
  truths:
    - "Notification permission requested on first launch"
    - "Local notification scheduled when new AF episode detected"
  artifacts:
    - path: "AFOne/Core/Notifications/NotificationService.swift"
      provides: "AF episode notification scheduling"
  key_links:
    - from: "NotificationService"
      to: "UNUserNotificationCenter"
      via: "Local notification scheduling"
    - from: "NotificationService"
      to: "HKHealthStore"
      via: "HKObserverQuery for background"
---

<objective>
Implement notification service for AF episode detection alerts.

Purpose: Users receive notification when AF episode is detected (via HealthKit background delivery or foreground check).
Output: Notification permission requested, notifications scheduled on new episodes.
</objective>

<context>
@.planning/phases/01-foundation-core-display/01-02-PLAN.md
@.planning/phases/01-foundation-core-display/01-02-SUMMARY.md (will exist after plan 02)

## Key Requirement

- NOTIF-01: User receives notification when AF episode is detected

## Important Notes from Research

- Background delivery via HKObserverQuery is UNRELIABLE
- Always implement foreground refresh on app launch as primary mechanism
- Use HKObserverQuery as supplement only
- Notifications should be informational, not alarming
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Notification service</name>
  <files>
    AFOne/Core/Notifications/NotificationService.swift
  </files>
  <action>
    Create NotificationService:
    
    **Request Permission:**
    - Request UNUserNotificationCenter authorization on first launch
    - Categories: AF_ALERT (view details action)
    
    **Schedule Notification (NOTIF-01):**
    - Check for new episodes on app launch (foreground)
    - If new episode since last check: schedule local notification
    - Title: "AF Episode Detected"
    - Body: "Your Apple Watch detected an irregular rhythm at [time]. Tap for details."
    - Sound: default
    - Action: "View" opens app to Episode detail
    
    **Background Handling:**
    - Register HKObserverQuery for atrialFibrillationBurden
    - Note: May be delayed/unreliable - always check on foreground too
    
    **Episode Duration Alert (NOTIF-02 - Phase 2):**
    - Mark as TODO for Phase 2 implementation
  </action>
  <verify>
    <automated>MISSING — Test notification scheduling after this task completes</automated>
  </verify>
  <done>
    Notification permission requested on first launch.
    New AF episodes trigger local notification.
    Notification opens app to episode detail.
    Background observer registered (with reliability caveat noted).
  </done>
</task>

</tasks>

<verification>
- NotificationService requests permission on first launch
- Local notification scheduled for new episodes
- Notification opens to episode detail
- HKObserverQuery registered (with reliability note)
</verification>

<success_criteria>
User receives notification when AF episode is detected. Permission requested and notification scheduled on new episode detection.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-display/01-06-SUMMARY.md`
</output>
