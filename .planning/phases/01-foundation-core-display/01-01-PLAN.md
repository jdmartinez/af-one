---
phase: 01-foundation-core-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [FOUN-01, FOUN-02, FOUN-03, FOUN-04, FOUN-05]
must_haves:
  truths:
    - "HealthKitService is the ONLY component touching HKHealthStore"
    - "App launches with 4-tab TabView navigation"
    - "Disclaimer shown on first launch and in Settings"
  artifacts:
    - path: "AFOne/Core/HealthKit/HealthKitService.swift"
      provides: "Singleton for all HealthKit queries"
      exports: ["requestAuthorization", "fetchAfBurden", "fetchEpisodes", "fetchHeartRateSamples", "fetchCurrentRhythmStatus"]
    - path: "AFOne/Models/"
      provides: "Domain models (RhythmEpisode, HeartRateReading, AFBurden)"
    - path: "AFOne/App/AFOneApp.swift"
      provides: "App entry point with TabView"
    - path: "AFOne/Shared/Components/DisclaimerView.swift"
      provides: "Not a medical device disclaimer"
  key_links:
    - from: "AFOne/App/AFOneApp.swift"
      to: "AFOne/Core/HealthKit/HealthKitService.swift"
      via: "@Environment injection"
    - from: "AFOne/App/ContentView.swift"
      to: "Features/"
      via: "TabView navigation"
---

<objective>
Create foundation: HealthKitService, domain models, app entry point with TabView, and disclaimer.

Purpose: Establishes all core infrastructure that downstream features depend on. HealthKitService is the ONLY component touching HKHealthStore.
Output: Working app shell with 4-tab navigation and required disclaimers.
</objective>

<context>
@.planning/1-CONTEXT.md
@.planning/research/STACK.md
@.planning/phases/01-foundation-core-display/01-RESEARCH.md

## Code Context

From 1-CONTEXT.md - Planned Project Structure:
```
AFOne/
├── App/
│   ├── AFOneApp.swift           # App entry, TabView root
│   └── ContentView.swift        # TabView with 4 tabs
├── Core/
│   ├── HealthKit/
│   │   ├── HealthKitService.swift       # Singleton, all HK interactions
│   │   ├── HealthKitTypes.swift         # Type definitions
│   │   └── HealthKitError.swift         # Error types
│   └── Analysis/
│       └── AFBurdenCalculator.swift
├── Features/
│   ├── Dashboard/
│   ├── Timeline/
│   ├── Episodes/
│   └── More/
├── Models/
│   ├── RhythmEpisode.swift
│   ├── HeartRateReading.swift
│   └── AFBurden.swift
└── Shared/
    ├── Components/
    │   ├── MetricCard.swift
    │   └── RhythmBlockView.swift
    └── Extensions/
        └── Date+Extensions.swift
```
</context>

<tasks>

<task type="auto">
  <name>task 1: Create HealthKitService and domain models</name>
  <files>
    AFOne/Core/HealthKit/HealthKitService.swift
    AFOne/Core/HealthKit/HealthKitTypes.swift
    AFOne/Core/HealthKit/HealthKitError.swift
    AFOne/Models/RhythmEpisode.swift
    AFOne/Models/HeartRateReading.swift
    AFOne/Models/AFBurden.swift
  </files>
  <action>
    Create HealthKitService as singleton that handles ALL HealthKit interactions:
    - requestAuthorization() async throws -> Bool
    - fetchAfBurden(from: Date, to: Date) async throws -> [AFBurden]
    - fetchEpisodes(from: Date, to: Date) async throws -> [RhythmEpisode]
    - fetchHeartRateSamples(from: Date, to: Date) async throws -> [HeartRateReading]
    - fetchCurrentRhythmStatus() async -> RhythmStatus
    - isHealthDataAvailable() -> Bool
    
    Create domain models:
    - RhythmEpisode: id, startDate, endDate, averageHR, peakHR, rhythmType
    - HeartRateReading: id, timestamp, bpm, source
    - AFBurden: date, percentage, sampleCount
    - RhythmStatus: enum (normal, af, unknown)
    
    Use @Observable + @MainActor for service. Use async/await with HKSampleQueryDescriptor (iOS 17+).
  </action>
  <verify>
    <automated>MISSING — Create test file after this task completes</automated>
  </verify>
  <done>
    HealthKitService is the only component that touches HKHealthStore.
    All 4 query methods implemented with async/await.
    Domain models conform to Codable for potential SwiftData persistence.
  </done>
</task>

<task type="auto">
  <name>task 2: Create app entry with TabView navigation</name>
  <files>
    AFOne/App/AFOneApp.swift
    AFOne/App/ContentView.swift
  </files>
  <action>
    Create AFOneApp.swift as entry point with:
    - @main struct
    - SwiftData ModelContainer (prepared for Phase 2)
    - HealthKitService environment injection
    
    Create ContentView.swift with TabView (4 tabs):
    - Tab 1: Dashboard (house.fill)
    - Tab 2: Timeline (chart.bar.xaxis)
    - Tab 3: Episodes (heart.circle)
    - Tab 4: More (ellipsis.circle) → Settings, Emergency
    
    Each tab gets its own NavigationStack for push navigation.
    Use .tabViewStyle(.page) for iPad-friendly behavior.
  </action>
  <verify>
    <automated>MISSING — Build test after this task completes</automated>
  </verify>
  <done>
    App launches with 4-tab TabView.
    Each tab has NavigationStack for drill-down.
    Tab icons: house.fill, chart.bar.xaxis, heart.circle, ellipsis.circle
  </done>
</task>

<task type="auto">
  <name>task 3: Add medical device disclaimer</name>
  <files>
    AFOne/Shared/Components/DisclaimerView.swift
  </files>
  <action>
    Create DisclaimerView component:
    - Bold "Not a Medical Device" headline
    - Clear explanation: "AFOne is for informational purposes only..."
    - Show on first launch BEFORE any data access
    - Show again in Settings tab
    - Use @AppStorage to track if user has acknowledged
    
    Include required FOUN-02 text from requirements.
  </action>
  <verify>
    <automated>grep -r "Not a medical device" AFOne/</automated>
  </verify>
  <done>
    Disclaimer shown on first launch.
    Disclaimer available in Settings tab.
    User can dismiss and acknowledge.
  </done>
</task>

</tasks>

<verification>
- HealthKitService is the only component importing HealthKit
- TabView has 4 tabs matching CONTEXT.md decisions
- Disclaimer text matches FOUN-02 requirement
</verification>

<success_criteria>
Phase 1 foundation complete: HealthKitService singleton created, domain models defined, app shell with 4-tab navigation, disclaimer in place.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-display/01-01-SUMMARY.md`
</output>
