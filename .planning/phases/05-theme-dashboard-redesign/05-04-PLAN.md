---
phase: 05-theme-dashboard-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Features/More/EmergencyView.swift
  - AFOne/Features/Reports/ReportView.swift
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Fix navigation issues - Phase 5 introduced .navigationBarBackButtonHidden(true) which completely hides back buttons in EmergencyView and ReportView. This fix removes those modifiers to restore proper navigation.
</objective>

<context>
@.planning/phases/05-theme-dashboard-redesign/05-UAT.md
</context>

<must_haves>
  truths:
    - "EmergencyView has a working back button"
    - "ReportView has a working back button"
  artifacts:
    - path: "AFOne/Features/More/EmergencyView.swift"
      provides: "Emergency view with navigation"
      contains: "navigationBarBackButtonHidden"
    - path: "AFOne/Features/Reports/ReportView.swift"
      provides: "Report view with navigation"
      contains: "navigationBarBackButtonHidden"
  key_links:
    - from: "EmergencyView"
      to: "MoreView"
      via: "navigation"
    - from: "ReportView"
      to: "MoreView"
      via: "navigation"
</must_haves>

<tasks>

<task type="auto">
  <name>task 1: Remove navigationBarBackButtonHidden from EmergencyView</name>
  <files>AFOne/Features/More/EmergencyView.swift</files>
  <action>
    Remove line 59: `.navigationBarBackButtonHidden(true)` from EmergencyView.swift.
    
    The original fix was intended to prevent duplicate back buttons, but it completely hides the back button instead. The correct approach is to remove this modifier entirely and let SwiftUI handle the navigation naturally.
  </action>
  <verify>
    <automated>grep -q "navigationBarBackButtonHidden" AFOne/Features/More/EmergencyView.swift && echo "FAIL: still contains modifier" || echo "PASS: modifier removed"</automated>
  </verify>
  <done>EmergencyView has a standard back button when navigated to from MoreView</done>
</task>

<task type="auto">
  <name>task 2: Remove navigationBarBackButtonHidden from ReportView</name>
  <files>AFOne/Features/Reports/ReportView.swift</files>
  <action>
    Remove line 53: `.navigationBarBackButtonHidden(true)` from ReportView.swift.
    
    This was added to prevent duplicate back buttons but hides it entirely. Also consider removing the Close button toolbar item (lines 30-34) since it was a workaround for the missing back button.
  </action>
  <verify>
    <automated>grep -q "navigationBarBackButtonHidden" AFOne/Features/Reports/ReportView.swift && echo "FAIL: still contains modifier" || echo "PASS: modifier removed"</automated>
  </verify>
  <done>ReportView has a standard back button when navigated to from MoreView</done>
</task>

</tasks>

<verification>
- Build succeeds on iOS Simulator
- Navigate to EmergencyView from MoreView - back button works
- Navigate to ReportView from MoreView - back button works
</verification>

<success_criteria>
- Both EmergencyView and ReportView have working back buttons
- No duplicate back buttons appear
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-dashboard-redesign/05-04-SUMMARY.md`
</output>
