---
phase: 05-theme-dashboard-redesign
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Shared/Extensions/Theme.swift
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "App displays correctly in both light and dark mode"
    - "cardShadow no longer breaks dark mode appearance"
    - "All hardcoded colors that need fixing are fixed"
  artifacts:
    - path: "AFOne/Shared/Extensions/Theme.swift"
      provides: "Adaptive theme colors"
      min_lines: 20
  key_links:
    - from: "DashboardView.swift"
      to: "Theme.swift"
      via: "Color.cardShadow, Color.cardBackground"
      pattern: "cardShadow|cardBackground"
---

<objective>
Fix dark/light theme support by updating the cardShadow color to work in dark mode.

Purpose: The current `Color.black.opacity(0.1)` shadow breaks in dark mode. Replace with material or remove shadow.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-theme-dashboard-redesign/05-CONTEXT.md

From Theme.swift (lines 20-22):
```swift
static var cardShadow: Color {
    Color.black.opacity(0.1)  // PROBLEM: This doesn't adapt to dark mode
}
```

Current system colors that already work (no changes needed):
- Color.cardBackground = Color(.systemBackground) - adapts automatically
- .green, .red, .orange - SwiftUI system colors adapt to dark mode
- AFStatusColor.color(for:) - returns system colors
- BurdenColor.color(for:) - returns system colors
</context>

<tasks>

<task type="auto">
  <name>task 1: Fix cardShadow for dark mode</name>
  <files>AFOne/Shared/Extensions/Theme.swift</files>
  <action>
1. Replace the `cardShadow` computed property (lines 20-22) to use an adaptive approach:
   - Option A: Use `.shadow(color: .primary.opacity(0.1), radius: 2, x: 0, y: 1)` 
   - Option B: Remove entirely since we'll use materials in dashboard
   - Option C: Use material: `.background(.ultraThinMaterial)` instead of shadow

2. Per CONTEXT.md locked decision: Use `.ultraThinMaterial` approach for dashboard cards. Since we won't need shadow in cards anymore, consider:
   - Removing cardShadow entirely if not used elsewhere
   - OR making it truly adaptive with `.primary.opacity(0.1)` as fallback

3. DO NOT create new asset catalog colors - use code-defined semantic colors only
</action>
  <verify>
    <automated>cd /Users/jdmartinez/Documents/Developer/af-one && xcodebuild -scheme AFOne -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - cardShadow fixed OR removed (no longer breaks dark mode)
    - Project compiles successfully
  </done>
</task>

</tasks>

<verification>
- [ ] Build succeeds on iOS Simulator
- [ ] No compilation warnings related to Color
</verification>

<success_criteria>
Theme.swift modified to remove the broken Color.black.opacity(0.1) shadow. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-dashboard-redesign/05-01-SUMMARY.md`
</output>
