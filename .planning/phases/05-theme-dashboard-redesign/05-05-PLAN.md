---
phase: 05-theme-dashboard-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/App/ContentView.swift
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Fix dynamic theme switching - App doesn't respond to color scheme changes while running. Need to ensure ContentView properly uses @Environment(\.colorScheme) and doesn't hardcode a color scheme.
</objective>

<context>
@.planning/phases/05-theme-dashboard-redesign/05-UAT.md
</context>

<must_haves>
  truths:
    - "App responds to dynamic theme changes while running"
  artifacts:
    - path: "AFOne/App/ContentView.swift"
      provides: "Main app view that should respond to colorScheme changes"
      contains: "colorScheme"
  key_links:
    - from: "ContentView"
      to: "System"
      via: "@Environment(\\.colorScheme)"
</must_haves>

<tasks>

<task type="auto">
  <name>task 1: Add dynamic colorScheme support to ContentView</name>
  <files>AFOne/App/ContentView.swift</files>
  <action>
    Read ContentView.swift to find:
    1. If there's a hardcoded colorScheme modifier (e.g., .colorScheme(.light))
    2. How the colorScheme environment is being used
    
    Fix by:
    - Remove any hardcoded colorScheme modifiers on ContentView or its ancestors
    - Ensure ContentView properly uses @Environment(\.colorScheme) if needed
    - The app should automatically respond to system colorScheme changes via SwiftUI's environment system
  </action>
  <verify>
    <automated>grep -q "colorScheme(\." AFOne/App/ContentView.swift && echo "FAIL: hardcoded colorScheme" || echo "PASS: no hardcoded colorScheme"</automated>
  </verify>
  <done>Switching between dark/light mode while app is running updates the UI immediately</done>
</task>

</tasks>

<verification>
- Build succeeds on iOS Simulator
- Test: Start app in light mode, switch to dark mode via system settings while app is open - app should update
</verification>

<success_criteria>
- App responds to dynamic theme changes while running
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-dashboard-redesign/05-05-SUMMARY.md`
</output>
