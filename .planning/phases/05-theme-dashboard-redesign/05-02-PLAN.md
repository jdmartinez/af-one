---
phase: 05-theme-dashboard-redesign
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Features/Dashboard/DashboardView.swift
  - AFOne/Features/Dashboard/MetricCardView.swift
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Dashboard cards use .ultraThinMaterial instead of shadows"
    - "Cards have 16pt rounded corners"
    - "Cards are full-width (not 2x2 grid)"
    - "Card headers show title on left, primary value on right"
    - "Colored circle icons (10pt) with SF Symbols"
    - "Period picker moved inside AF Burden card header"
    - "Toolbar reload button removed (keep pull-to-refresh)"
  artifacts:
    - path: "AFOne/Features/Dashboard/DashboardView.swift"
      provides: "Apple Health-style dashboard"
      min_lines: 100
    - path: "AFOne/Features/Dashboard/MetricCardView.swift"
      provides: "Redesigned metric card component"
      min_lines: 30
  key_links:
    - from: "DashboardView.metricsSection"
      to: "MetricCardView"
      via: "LazyVStack of full-width cards"
      pattern: "MetricCardView"
---

<objective>
Redesign dashboard with Apple Health-style UI: materials, 16pt corners, full-width cards, header layout, colored icons.

Purpose: Match Apple Health app aesthetics for a native iOS feel that works in both light and dark mode.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-theme-dashboard-redesign/05-CONTEXT.md

Apple Health Card Anatomy:
```
┌────────────────────────────────────────────────┐
│  Heart Rate              72  bpm              │  ← Header: title | value unit
├────────────────────────────────────────────────┤
│  ○ Resting            62  bpm                 │  ← Row: colored circle | label | value
│  ○ Walking            98  bpm                 │    Secondary values in smaller font
│  ○ Cardio             120  bpm                │
└────────────────────────────────────────────────┘
```

Implementation Notes from CONTEXT.md:
- Card background: `.background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 16))`
- Padding: 16pt horizontal, 12pt vertical
- No shadows - materials provide depth
- Metric cards: Convert from 2x2 grid to full-width stacked cards
- Icon format: `Circle()` with 10pt diameter, filled with semantic color, SF Symbol inside
- Card header layout: Title on left, primary value on right (same line)
- Period picker goes inside AF Burden card header
- Remove toolbar button (arrow.clockwise), keep pull-to-refresh
</context>

<tasks>

<task type="auto">
  <task>task 1: Redesign MetricCardView with Apple Health style</task>
  <files>AFOne/Features/Dashboard/MetricCardView.swift</files>
  <action>
1. Update MetricCardView to Apple Health style:
   - Change from `.cornerRadius(12)` to `.clipShape(RoundedRectangle(cornerRadius: 16))`
   - Replace `.shadow(color: .cardShadow, ...)` with `.background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 16))`
   - Add colored circle icon (10pt Circle filled with semantic color, SF Symbol inside)
   - Card header layout: Title left, value right (same line)
   - Use `.system(size: 34, weight: .bold)` for primary values

2. Icon format:
   - Create Circle() with 10pt diameter
   - Fill with semantic color (passed via `color` parameter)
   - Place SF Symbol inside with white foreground

3. Do NOT change the function signature - keep existing parameters but use them differently
</action>
  <verify>
    <automated>grep -l "ultraThinMaterial" AFOne/Features/Dashboard/MetricCardView.swift</automated>
  </verify>
  <done>
    - MetricCardView uses ultraThinMaterial
    - 16pt corner radius
    - Colored circle icons
    - Header layout (title left, value right)
  </done>
</task>

<task type="auto">
  <task>task 2: Convert metrics section from 2x2 grid to full-width cards</task>
  <files>AFOne/Features/Dashboard/DashboardView.swift</files>
  <action>
1. Find `metricsSection` (LazyVGrid, lines 164-199)
2. Replace LazyVGrid with LazyVStack for full-width cards:
   ```swift
   LazyVStack(spacing: 12) {
       MetricCardView(...)
       MetricCardView(...)
       MetricCardView(...)
       MetricCardView(...)
   }
   ```

3. Update each MetricCardView to show colored circle icon (passed to icon parameter)
</action>
  <verify>
    <automated>grep -c "MetricCardView" AFOne/Features/Dashboard/DashboardView.swift</automated>
  </verify>
  <done>
    - Metrics displayed as full-width stacked cards (not 2x2 grid)
  </done>
</task>

<task type="auto">
  <task>task 3: Move period picker inside AF Burden card</task>
  <files>AFOne/Features/Dashboard/DashboardView.swift</files>
  <action>
1. Find `burdenSection` (lines 202-251)
2. Currently has period picker at top (lines 204-222)
3. Move the Picker inside the card header, next to "AF Burden" title:
   ```swift
   HStack {
       Text("AF Burden")
           .font(.headline)
       Picker("Period", selection: $viewModel.selectedPeriod) { ... }
           .pickerStyle(.segmented)
           .frame(width: 120)  // Smaller width since it's inline
       Spacer()
   }
   ```

4. Keep the period picker functionality working (onChange trigger)
</action>
  <verify>
  <automated>grep -A5 "burdenSection" AFOne/Features/Dashboard/DashboardView.swift | grep -c "Picker"</automated>
  </verify>
  <done>
    - Period picker inside AF Burden card header
  </done>
</task>

<task type="auto">
  <task>task 4: Remove toolbar reload button</task>
  <files>AFOne/Features/Dashboard/DashboardView.swift</files>
  <action>
1. Find toolbar definition (lines 20-32)
2. Remove the ToolbarItem with arrow.clockwise button (lines 27-31)
3. Keep:
   - NavigationLink to EmergencyView (lines 21-26)
   - Pull-to-refresh via .refreshable (lines 33-38) - KEEP THIS
4. Add .help("description") to EmergencyView toolbar button for accessibility
</action>
  <verify>
  <automated>grep -c "arrow.clockwise" AFOne/Features/Dashboard/DashboardView.swift</automated>
  </verify>
  <done>
    - No arrow.clockwise toolbar button
    - Pull-to-refresh still works
  </done>
</task>

</tasks>

<verification>
- [ ] All 4 cards in metrics section use full-width layout
- [ ] Cards have 16pt corners and ultraThinMaterial
- [ ] Period picker inside AF Burden card
- [ ] No toolbar reload button
- [ ] Build compiles
</verification>

<success_criteria>
Dashboard redesigned with Apple Health aesthetics. All UI-02 requirements met.
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-dashboard-redesign/05-02-SUMMARY.md`
</output>
