---
phase: 05-theme-dashboard-redesign
plan: '03'
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Features/More/EmergencyView.swift
  - AFOne/Features/Reports/ReportView.swift
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "EmergencyView has no duplicate back button"
    - "ReportView has no duplicate back button"
    - "Both views are accessible via NavigationLink from MoreView"
  artifacts:
    - path: "AFOne/Features/More/EmergencyView.swift"
      provides: "Fixed navigation"
      min_lines: 60
    - path: "AFOne/Features/Reports/ReportView.swift"
      provides: "Fixed navigation"
      min_lines: 100
  key_links:
    - from: "MoreView"
      to: "EmergencyView"
      via: "NavigationLink"
      pattern: "NavigationLink.*EmergencyView"
---

<objective>
Fix duplicate back button issue in EmergencyView and ReportView.

Purpose: When navigating from MoreView via NavigationLink, the back button appears twice. Hide the native back button in these destination views.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-theme-dashboard-redesign/05-CONTEXT.md

From CONTEXT.md:
- UI-03.1: Fix More > Emergency Information duplicate back button
- UI-03.2: Fix More > Clinical Report duplicate back button
- Both views should hide the back button explicitly since they're accessed via NavigationLink from MoreView

Implementation: Add `.navigationBarBackButtonHidden(true)` to the root view in both files.
</context>

<tasks>

<task type="auto">
  <name>task 1: Fix EmergencyView duplicate back button</name>
  <files>AFOne/Features/More/EmergencyView.swift</files>
  <action>
1. Find the body property in EmergencyView (line 6+)
2. Add `.navigationBarBackButtonHidden(true)` modifier to the root view (List)

Current structure:
```swift
var body: some View {
    List { ... }
    .navigationTitle("Emergency")
}
```

Updated structure:
```swift
var body: some View {
    List { ... }
    .navigationTitle("Emergency")
    .navigationBarBackButtonHidden(true)
}
```

The view is accessed via NavigationLink from MoreView, so we need to hide the back button.
</action>
  <verify>
    <automated>grep -n "navigationBarBackButtonHidden" AFOne/Features/More/EmergencyView.swift</automated>
  </verify>
  <done>
    - EmergencyView has navigationBarBackButtonHidden(true)
  </done>
</task>

<task type="auto">
  <name>task 2: Fix ReportView duplicate back button</name>
  <files>AFOne/Features/Reports/ReportView.swift</files>
  <action>
1. Find the body property in ReportView (lines 8-53)
2. Add `.navigationBarBackButtonHidden(true)` modifier to the NavigationStack

Current structure:
```swift
var body: some View {
    NavigationStack {
        ...
    }
}
```

Updated structure:
```swift
var body: some View {
    NavigationStack {
        ...
    }
    .navigationBarBackButtonHidden(true)
}
```

Note: ReportView also has its own close button (cancellationAction toolbar), so hiding the back button is appropriate.
</action>
  <verify>
    <automated>grep -n "navigationBarBackButtonHidden" AFOne/Features/Reports/ReportView.swift</automated>
  </verify>
  <done>
    - ReportView has navigationBarBackButtonHidden(true)
  </done>
</task>

</tasks>

<verification>
- [ ] Both views have navigationBarBackButtonHidden(true)
- [ ] Build compiles
</verification>

<success_criteria>
Navigation fixed - no duplicate back buttons in EmergencyView or ReportView.
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-dashboard-redesign/05-03-SUMMARY.md`
</output>
