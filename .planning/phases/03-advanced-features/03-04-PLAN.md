---
phase: 03-advanced-features
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Features/More/MoreView.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can access clinical report feature from navigation"
  artifacts:
    - path: "AFOne/Features/More/MoreView.swift"
      provides: "Navigation access to ReportView"
      contains: "NavigationLink.*ReportView"
  key_links:
    - from: "MoreView.swift"
      to: "ReportView.swift"
      via: "NavigationLink"
      pattern: "NavigationLink.*ReportView"
---

<objective>
Add navigation link to ReportView in MoreView.

Purpose: Users can access the clinical report feature that was implemented in plan 03-03 but was never wired to the app's navigation.
Output: NavigationLink to ReportView in MoreView
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-advanced-features/03-VERIFICATION.md

<!-- Gap: User cannot access clinical report feature because no navigation link exists -->
<!-- Root cause: Plan 03-03 implemented ReportView but didn't add navigation to it -->

<!-- ReportView interface -->
From AFOne/Features/Reports/ReportView.swift:
```swift
struct ReportView: View {
    @StateObject private var viewModel = ReportViewModel()
    // Displays clinical report with period picker and ShareLink
    // Uses NavigationStack, auto-generates report on appear
}
```

<!-- MoreView structure -->
From AFOne/Features/More/MoreView.swift:
```swift
struct MoreView: View {
    // Uses NavigationStack with List sections:
    // - Section("Health"): Emergency Information
    // - Section("Settings"): App Settings
    // - Section("About"): About AFOne
}
```
</context>

<tasks>

<task type="auto">
  <name>task 1: Add ReportView navigation to MoreView</name>
  <files>AFOne/Features/More/MoreView.swift</files>
  <action>
    In MoreView, add a new NavigationLink in the "Health" section that navigates to ReportView.
    
    Placement: Add after Emergency Information in the Health section.
    
    Code to add:
    ```swift
    NavigationLink(destination: ReportView()) {
        Label("Clinical Report", systemImage: "doc.text.fill")
    }
    ```
    
    Keep existing Emergency Information navigation link.
  </action>
  <verify>
    <automated>grep -n "NavigationLink.*ReportView" AFOne/Features/More/MoreView.swift</automated>
  </verify>
  <done>MoreView has NavigationLink to ReportView in Health section</done>
</task>

</tasks>

<verification>
- [ ] MoreView contains NavigationLink to ReportView
- [ ] Link appears in Health section
- [ ] Uses appropriate icon (doc.text.fill or similar)
- [ ] Existing navigation links (Emergency, Settings, About) still work
</verification>

<success_criteria>
User can navigate to Clinical Report from More > Health section
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-features/03-04-SUMMARY.md`
</output>
