---
phase: 07-dashboard-redesign-specification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Shared/Extensions/Theme.swift
  - AFOne/Assets.xcassets
autonomous: true
requirements:
  - UI-01
  - UI-02
---

<objective>
Implement the color system foundation: create custom brand color assets in Assets.xcassets and update Theme.swift to use semantic iOS colors per SPECIFICATION.md Section 2.
</objective>

<context>
@.planning/phases/07-dashboard-redesign-specification/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign-specification/07-SPECIFICATION.md
@AFOne/Shared/Extensions/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>task 1: Create custom color assets in Assets.xcassets</name>
  <files>AFOne/Assets.xcassets</files>
  <action>
Create five Color Set assets in AFOne/Assets.xcassets with All Appearances, Dark, and High Contrast variants:

1. AFOne/RhythmSinusal - green clinical indicator for SR state
2. AFOne/RhythmAF - red clinical indicator for AF state  
3. AFOne/BurdenLow - green for burden < 5.5%
4. AFOne/BurdenMid - amber/orange for burden 5.5-10.9%
5. AFOne/BurdenHigh - red for burden ≥ 11%

Each color set must have:
- Any Appearance (Light): baseline color
- Dark: dark mode variant  
- High Contrast: accessibility variant

Use Xcode to create these via asset catalog structure.
  </action>
  <verify>
  <automated>ls AFOne/Assets.xcassets/ | grep -E "Rhythm|Burden"</automated>
  </verify>
  <done>Five Color Set directories exist in Assets.xcassets with appearance variants</done>
</task>

<task type="auto">
  <name>task 2: Update Theme.swift with semantic color accessors</name>
  <files>AFOne/Shared/Extensions/Theme.swift</files>
  <action>
Replace Theme.swift content to use iOS semantic colors per SPECIFICATION.md Section 2:

1. Keep existing Color extensions but ensure they use `Color(uiColor:)` initializer
2. Add AFOne brand color accessors using the new Assets.xcassets colors:
   - `AFOne.RhythmSinusal` - accesses AFOne/RhythmSinusal color set
   - `AFOne.RhythmAF` - accesses AFOne/RhythmAF color set
   - `AFOne.BurdenLow` - accesses AFOne/BurdenLow color set
   - `AFOne.BurdenMid` - accesses AFOne/BurdenMid color set
   - `AFOne.BurdenHigh` - accesses AFOne/BurdenHigh color set

3. Add color functions that match SPEC thresholds:
   - `burdenColor(for percentage: Double)` - returns BurdenLow/Mid/High based on 5.5% and 11% thresholds

4. Use Color extension pattern: `extension Color { static let afOne = AFOne() }` nested type

Example pattern:
```swift
extension Color {
    static let afOne = AFOneColors()
    
    struct AFOneColors {
        let rhythmSinusal = Color("AFOne/RhythmSinusal")
        let rhythmAF = Color("AFOne/RhythmAF")
        // ... etc
    }
}
```
  </action>
  <verify>
  <automated>grep -l "AFOne" AFOne/Shared/Extensions/Theme.swift</automated>
  </verify>
  <done>Theme.swift contains AFOne brand color accessors and burden color function</done>
</task>

<task type="auto">
  <name>task 3: Verify no hardcoded colors remain</name>
  <files>AFOne/Features/Dashboard/DashboardView.swift</files>
  <action>
Search for and fix any hardcoded Color values in DashboardView.swift:

1. Replace `.green`, `.red`, `.orange`, `.yellow`, `.blue`, `.gray` with semantic equivalents
2. Replace any hex/RGB values with semantic or AFOne brand colors
3. Ensure all color references use either:
   - Semantic system colors (`.primary`, `.secondary`, `.systemRed`)
   - AFOne brand colors (`Color.afOne.rhythmSinusal`, etc.)

Current code to fix:
- `statusColor` computed property uses `.green`, `.red`, `.gray`
- `burdenColor` uses system colors instead of AFOne brand
  </action>
  <verify>
  <automated>grep -E "Color\.(green|red|orange|yellow|blue|gray|#)" AFOne/Features/Dashboard/DashboardView.swift || echo "No hardcoded colors found"</automated>
  </verify>
  <done>DashboardView.swift uses only semantic or AFOne brand colors</done>
</task>

</tasks>

<verification>
- [ ] Five AFOne color sets exist in Assets.xcassets with appearance variants
- [ ] Theme.swift provides AFOne brand color accessors
- [ ] burdenColor(for:) returns correct color based on thresholds
- [ ] DashboardView.swift has no hardcoded system colors
</verification>

<success_criteria>
Phase 7 zone implementation can use semantic colors and AFOne brand colors consistently across all views.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-redesign-specification/07-02-SUMMARY.md`
</output>
