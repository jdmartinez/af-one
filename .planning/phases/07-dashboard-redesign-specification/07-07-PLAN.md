---
phase: 07-dashboard-redesign-specification
plan: 07
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - AFOne/Features/Dashboard/SymptomCaptureButton.swift
  - AFOne/Features/Dashboard/DashboardView.swift
autonomous: true
requirements:
  - UI-02
---

<objective>
Implement Zone 5 - Symptom Capture Button per SPECIFICATION.md Section 8. Full-width tappable element with sheet presentation.
</objective>

<context>
@.planning/phases/07-dashboard-redesign-specification/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign-specification/07-SPECIFICATION.md

**Symptom Capture Button Specification from SPEC.md Section 8:**
- Full-width tappable element at bottom of scroll content
- Implemented as Button with custom ButtonStyle (not ZStack + TapGesture)

**SR State (single button):**
- Background: secondarySystemBackground with RhythmSinusal tint at 8% opacity
- Border: 1pt separator color
- Corner radius: 18

**Layout (HStack, spacing 14):**
1. Icon container: 44x44pt, tertiarySystemFill background, cornerRadius 14
   - Icon: plus.circle.fill, title3, systemBlue
2. VStack(alignment: .leading, spacing: 2):
   - "Capturar síntoma" - headline, primary
   - "Registrar + iniciar ECG de 30s" - subheadline, secondary
3. Trailing: chevron.right, caption, tertiary

**Touch target:** 
- Minimum 44pt height
- Use .contentShape(RoundedRectangle(cornerRadius: 18)) for full tap area

**Action:**
- Presents modal sheet (.sheet(isPresented:))
- Not full-screen push

**AF Active State:**
- Show second button above primary button
- Second button: AF color tint overlay
- Label: "Episodio en curso — Capturar contexto"
- Sub-label: "Registrar síntomas durante el episodio activo"
</context>

<tasks>

<task type="auto">
<parameter name="name>task 1: Create SymptomCaptureButton component</name>
  <files>AFOne/Features/Dashboard/SymptomCaptureButton.swift</files>
  <action>
Create SymptomCaptureButton.swift implementing Section 8 of SPEC.md:

1. Accept showLogSheet: Binding<Bool>, isAFActive: Bool parameters

2. **SR State button:**
   - Button with custom ButtonStyle or wrapped HStack
   - Background: Color.secondarySystemBackground + Color.afOne.rhythmSinusal.opacity(0.08) overlay
   - Border: 1pt via .overlay + stroke
   - Corner radius: 18
   
3. **Icon container:**
   - 44x44pt frame
   - Color.tertiarySystemFill background
   - RoundedRectangle(cornerRadius: 14)
   - Image(systemName: "plus.circle.fill"), .title3, .systemBlue

4. **Labels:**
   - "Capturar síntoma" - .headline, .primary
   - "Registrar + iniciar ECG de 30s" - .subheadline, .secondary
   
5. **Trailing chevron:**
   - Image(systemName: "chevron.right")
   - .caption, .tertiary

6. **Touch target:**
   - .frame(minHeight: 44)
   - .contentShape(RoundedRectangle(cornerRadius: 18))

7. **AF Active state:**
   - Show additional button above primary
   - Use Color.afOne.rhythmAF.opacity(0.08) for tint
   - Label: "Episodio en curso — Capturar contexto"
   - Sub-label: "Registrar síntomas durante el episodio activo"
  </action>
  <verify>
  <automated>ls AFOne/Features/Dashboard/SymptomCaptureButton.swift</automated>
  </verify>
  <done>SymptomCaptureButton.swift exists with both states</done>
</task>

<task type="auto">
  <name>task 2: Add accessibility support</name>
  <files>AFOne/Features/Dashboard/SymptomCaptureButton.swift</files>
  <action>
Add accessibility:

1. Button gets .accessibilityLabel("Capture symptom")
2. Button gets .accessibilityHint("Opens form to record symptoms and start 30-second ECG")
3. AF Active button gets appropriate label: "Capture symptom during episode"
4. Minimum 44x44pt touch target ensured
  </action>
  <verify>
  <automated>grep -l "accessibility" AFOne/Features/Dashboard/SymptomCaptureButton.swift</automated>
  </verify>
  <done>Accessibility labels added</done>
</task>

<task type="auto">
  <name>task 3: Integrate into DashboardView and wire sheet</name>
  <files>AFOne/Features/Dashboard/DashboardView.swift</files>
  <action>
Integrate SymptomCaptureButton into DashboardView:

1. Position at bottom of ScrollView content (Zone 5)
2. Add bottom padding: 32pt (clear home indicator)
3. Wire showLogSheet binding to existing sheet presentation
4. Pass isAFActive based on current episode state

5. Remove existing FAB (floating action button) implementation
   - Current ZStack with circular button at bottom-right
   - Replace with full-width button at bottom of scroll
  </action>
  <verify>
  <automated>grep -l "SymptomCaptureButton" AFOne/Features/Dashboard/DashboardView.swift</automated>
  </verify>
  <done>DashboardView replaces FAB with SymptomCaptureButton</done>
</task>

</tasks>

<verification>
- [ ] Button renders with correct layout and spacing
- [ ] Touch target meets 44pt minimum
- [ ] Sheet presentation works
- [ ] AF Active state shows second button
- [ ] Accessibility labels present
</verification>

<success_criteria>
Symptom capture easily accessible. User can quickly log symptoms with a single tap.
</success_criteria>

<output>
After completion, update `.planning/phases/07-dashboard-redesign-specification/07-07-SUMMARY.md`
</output>
