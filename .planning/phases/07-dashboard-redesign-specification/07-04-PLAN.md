---
phase: 07-dashboard-redesign-specification
plan: 04
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - AFOne/Features/Dashboard/BurdenCardView.swift
  - AFOne/Features/Dashboard/DashboardView.swift
autonomous: true
requirements:
  - UI-02
---

<objective>
Implement Zone 2 - AF Burden Card per SPECIFICATION.md Section 5. Shows AF burden percentage with time window selector and progress bar with threshold markers.
</objective>

<context>
@.planning/phases/07-dashboard-redesign-specification/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign-specification/07-SPECIFICATION.md
@AFOne/Shared/Extensions/Theme.swift

**Burden Card Specification from SPEC.md Section 5:**
- Card: RoundedRectangle(cornerRadius: 20), secondarySystemBackground, 20pt padding, 16pt margin
- Time window selector: Picker with .segmentedPickerStyle(), options: 24h / 7d / 30d
- Primary value: 52pt bold rounded font, color based on threshold
- Threshold badge: "↓ ASSERT <5.5%" or similar
- Progress bar: 6pt height, max at 11% = 100% width
- Threshold tick marks at 5.5% (50%) and 11% (100%)
- Legend: "Bajo riesgo", "≥5.5% ASSERT", "≥11% Alto"
- Delta row: "+X% vs semana pasada"

**Burden Color Logic:**
- < 5.5%: BurdenLow (green)
- 5.5-10.9%: BurdenMid (amber)
- ≥ 11%: BurdenHigh (red)
</context>

<tasks>

<task type="auto">
  <name>task 1: Create BurdenCardView component</name>
  <files>AFOne/Features/Dashboard/BurdenCardView.swift</files>
  <action>
Create BurdenCardView.swift implementing Section 5 of SPEC.md:

1. Accept burdenPercentage: Double, selectedPeriod: Binding<TimePeriod>, burdenTrend: Double parameters

2. **Time window selector:**
   - Picker with .segmentedPickerStyle()
   - Options: "24h", "7d", "30d"
   - Use existing TimePeriod enum or create local option set

3. **Primary value display:**
   - Number: .system(size: 52, weight: .bold, design: .rounded)
   - Unit: "%" - .title3, secondary, baseline-aligned
   - Color: Based on burdenColor(for:) from Theme.swift

4. **Threshold badge:**
   - Show "<5.5%" or "5.5-10.9%" or "≥11%" text
   - Background: burden color at 15% opacity
   - Text: burden color at 70% opacity
   - Capsule clip shape

5. **Progress bar:**
   - GeometryReader-based, 6pt height
   - Background: systemFill
   - Fill: LinearGradient from BurdenLow to active burden color
   - Width: (burden / 11.0) * maxWidth, capped at 100%

6. **Threshold tick marks:**
   - At 50% position (5.5%): Rectangle 2pt wide, 12pt tall, BurdenMid at 60% opacity
   - At 100% position (11%): Rectangle 2pt wide, 12pt tall, BurdenHigh at 60% opacity

7. **Legend:**
   - Three Label views inline: "Bajo riesgo", "≥5.5% ASSERT", "≥11% Alto"
   - Colored dots from burden color assets
   - .caption2, tertiary

8. **Delta row:**
   - Divider then text row
   - Format: "+X% vs semana pasada · N episodios hoy"
   - Delta uses RhythmAF if positive, RhythmSinusal if negative
   - .caption, tertiary
  </action>
  <verify>
  <automated>ls AFOne/Features/Dashboard/BurdenCardView.swift</automated>
  </verify>
  <done>BurdenCardView.swift exists with all specified elements</done>
</task>

<task type="auto">
  <name>task 2: Add accessibility support</name>
  <files>AFOne/Features/Dashboard/BurdenCardView.swift</files>
  <action>
Add accessibility labels and hints:

1. Time window picker: .accessibilityLabel("Select time period for burden calculation")
2. Burden value: .accessibilityValue("X percent") 
3. Threshold badge: .accessibilityLabel("Risk category: X")
4. Progress bar: .accessibilityLabel("Burden progress: X percent of maximum")
5. Use @ScaledMetric for any fixed sizes that need to scale with Dynamic Type

Ensure all text uses Dynamic Type text styles.
  </action>
  <verify>
  <automated>grep -l "accessibility" AFOne/Features/Dashboard/BurdenCardView.swift</automated>
  </verify>
  <done>All elements have accessibility labels</done>
</task>

<task type="auto">
  <name>task 3: Integrate BurdenCardView into DashboardView</name>
  <files>AFOne/Features/Dashboard/DashboardView.swift</files>
  <action>
Replace existing burdenSection with BurdenCardView:

1. Pass burdenPercentage from view model
2. Bind selectedPeriod to view model's period selection
3. Calculate burdenTrend (delta vs previous period) for delta row

4. Position: After HeroCardView in the VStack (Zone 2)
5. Maintain 12pt vertical gap between cards
  </action>
  <verify>
  <automated>grep -l "BurdenCardView" AFOne/Features/Dashboard/DashboardView.swift</automated>
  </verify>
  <done>DashboardView integrates BurdenCardView</done>
</task>

</tasks>

<verification>
- [ ] Segmented picker switches between 24h/7d/30d
- [ ] Burden value color matches threshold
- [ ] Progress bar fills proportionally up to 11%
- [ ] Threshold tick marks visible at correct positions
- [ ] Delta row shows correct color based on trend direction
</verification>

<success_criteria>
AF Burden displayed with clear clinical context. User understands their burden level and how it compares to clinical thresholds.
</success_criteria>

<output>
After completion, update `.planning/phases/07-dashboard-redesign-specification/07-04-SUMMARY.md`
</output>
