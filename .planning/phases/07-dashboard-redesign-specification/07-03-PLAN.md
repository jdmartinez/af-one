---
phase: 07-dashboard-redesign-specification
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - AFOne/Features/Dashboard/DashboardView.swift
  - AFOne/Features/Dashboard/HeroCardView.swift
autonomous: true
requirements:
  - UI-02
---

<objective>
Implement Zone 1 - Hero Card per SPECIFICATION.md Section 4. The Hero Card is the dominant element showing current rhythm status with SR and AF Active states.
</objective>

<context>
@.planning/phases/07-dashboard-redesign-specification/07-CONTEXT.md
@.planning/phases/07-dashboard-redesign-specification/07-SPECIFICATION.md
@AFOne/Shared/Extensions/Theme.swift

**Hero Card Specification from SPEC.md Section 4:**
- Container: RoundedRectangle(cornerRadius: 20), secondarySystemBackground with gradient overlay
- Padding: 20pt horizontal, 18pt vertical
- Margin: 16pt from edges
- SR State: Rhythm dot (pulsing), "Ritmo Sinusal" label, confidence badge, stats row (3 columns)
- AF Active State: Red gradient, "AF in progress" label, "Confirmed" badge, episode banner with timer + emergency button
</context>

<tasks>

<task type="auto">
  <name>task 1: Create HeroCardView component</name>
  <files>AFOne/Features/Dashboard/HeroCardView.swift</files>
  <action>
Create HeroCardView.swift implementing Section 4 of SPEC.md:

1. Accept @Binding or @State for isAFActive (whether AF episode is in progress)
2. Accept currentHR, lastEpisodeDuration, episodesToday, confidenceLevel parameters
3. Accept episodeStartDate for timer calculation

**SR State rendering:**
- Section label: "CURRENT RHYTHM" - .caption2, uppercase, tertiary
- Rhythm indicator: 10pt circle with Color.afOne.rhythmSinusal, pulsing scale animation
- Glow effect: .shadow(color: Color.afOne.rhythmSinusal, radius: 6)
- Rhythm label: "Ritmo Sinusal" - .title, semibold
- Confidence badge: "Alta confianza" - .caption, secondarySystemFill background
- Stats row: 3 equal columns (currentHR, lastEpisode, episodesToday) with Divider separators

**AF Active State rendering:**
- Replace rhythm dot color with Color.afOne.rhythmAF, glow radius 8
- Replace rhythm label with "Fibrilación Auricular"
- Replace confidence badge with "Confirmado"
- Show episode banner between rhythm row and stats row:
  - "ACTIVE EPISODE" label in AF color
  - Timer counting elapsed time (monospaced .title font)
  - "From last Apple Watch data" disclosure label
  - "From HH:MM" onset time
  - Emergency button: "Urgencias" with AF color background, opens tel://112

**Geometry:**
- RoundedRectangle(cornerRadius: 20)
- Background: Color.secondarySystemBackground with linear gradient overlay (10-15% opacity brand color)
- Padding: 20pt horizontal, 18pt vertical
  </action>
  <verify>
  <automated>ls AFOne/Features/Dashboard/HeroCardView.swift</automated>
  </verify>
  <done>HeroCardView.swift exists with SR and AF Active state implementations</done>
</task>

<task type="auto">
  <name>task 2: Add pulsing animation with accessibility support</name>
  <files>AFOne/Features/Dashboard/HeroCardView.swift</files>
  <action>
Add the pulsing rhythm dot animation with Reduce Motion support:

1. Use withAnimation with repeatForever(autoreverses: true) for pulsing
2. Scale effect: 1.0 → 0.85, 2s ease-in-out
3. Wrap in @Environment(\.accessibilityReduceMotion) check:
   - If reduceMotion is true: show static filled circle
   - If false: show pulsing animation

Add accessibility:
- .accessibilityLabel("Current heart rhythm status")
- .accessibilityValue() that reads either "Sinus rhythm" or "Atrial fibrillation in progress"
- For episode timer: .accessibilityValue() reads elapsed time in natural language
  </action>
  <verify>
  <automated>grep -l "accessibilityReduceMotion\|accessibilityLabel" AFOne/Features/Dashboard/HeroCardView.swift</automated>
  </verify>
  <done>Animation respects accessibilityReduceMotion, proper VoiceOver labels added</done>
</task>

<task type="auto">
  <name>task 3: Integrate HeroCardView into DashboardView</name>
  <files>AFOne/Features/Dashboard/DashboardView.swift</files>
  <action>
Replace the existing statusCard in DashboardView with HeroCardView:

1. Pass required parameters from DashboardViewModel:
   - isAFActive: based on whether an episode is currently in progress
   - currentHR: current heart rate value
   - lastEpisodeDuration: formatted duration string
   - episodesToday: count of episodes today
   - confidenceLevel: "Alta" or "Confirmado" based on data quality
   - episodeStartDate: start date of active episode (if any)

2. Remove old statusCard implementation
3. Keep the ScrollView structure but replace content with zones

4. Update timestamps to use .caption2, .quaternary with colored dot
   </action>
  <verify>
  <automated>grep -l "HeroCardView" AFOne/Features/Dashboard/DashboardView.swift</automated>
  </verify>
  <done>DashboardView uses HeroCardView component</done>
</task>

</tasks>

<verification>
- [ ] HeroCardView renders correct SR state with pulsing dot
- [ ] HeroCardView renders correct AF Active state with episode banner
- [ ] Animation respects accessibilityReduceMotion
- [ ] Emergency call button opens tel://112
- [ ] Timestamp footer shows with colored dot
</verification>

<success_criteria>
Hero card displays current rhythm status unambiguously. Physician can understand patient's state in under 3 seconds.
</success_criteria>

<output>
After completion, update `.planning/phases/07-dashboard-redesign-specification/07-03-SUMMARY.md`
</output>
