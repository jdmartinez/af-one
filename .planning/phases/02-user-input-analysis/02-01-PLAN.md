---
phase: 02-user-input-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AFOne/Models/SymptomLog.swift
  - AFOne/Models/TriggerLog.swift
  - AFOne/AFOneApp.swift
autonomous: true
requirements:
  - SYM-01
  - SYM-02
  - SYM-03
  - TRIG-01
  - TRIG-02

must_haves:
  truths:
    - "User can log symptoms when feeling unusual"
    - "Each symptom record has automatic timestamp"
    - "User can view complete symptom history"
    - "User can log lifestyle triggers"
  artifacts:
    - path: "AFOne/Models/SymptomLog.swift"
      provides: "SwiftData model for symptom persistence"
      exports: "SymptomLog @Model, SymptomType enum"
    - path: "AFOne/Models/TriggerLog.swift"
      provides: "SwiftData model for trigger persistence"
      exports: "TriggerLog @Model, TriggerType enum"
    - path: "AFOne/AFOneApp.swift"
      provides: "ModelContainer setup for SwiftData"
      updates: "Add modelContainer(for:) to WindowGroup"
  key_links:
    - from: "SymptomLog"
      to: "SwiftData ModelContext"
      via: "ModelContainer from App"
    - from: "TriggerLog"
      to: "SwiftData ModelContext"
      via: "ModelContainer from App"
---

<objective>
Create SwiftData models for symptom and trigger logging. This is the foundation for all Phase 2 user input features.

Purpose: Establish persistence layer for user-entered data using Apple's recommended SwiftData @Model pattern
Output: SymptomLog and TriggerLog models ready for use by logging UI
</objective>

<context>
@.planning/phases/02-user-input-analysis/02-CONTEXT.md
@.planning/phases/02-user-input-analysis/02-RESEARCH.md
@.planning/phases/01-foundation-core-display/01-06-SUMMARY.md

## Key Decisions from CONTEXT.md

- 6 symptom types: palpitations, anxiety, dizziness, fatigue, shortness of breath, chest discomfort
- 6 trigger types: alcohol, caffeine, stress, poor sleep, heavy meals, intense exercise
- SwiftData @Model macro for persistence
- Offline-first, no cloud sync
</context>

<tasks>

<task type="auto">
  <name>task 1: Create SymptomLog SwiftData model</name>
  <files>AFOne/Models/SymptomLog.swift</files>
  <action>
Create SwiftData @Model for symptom logging:

```swift
import SwiftData

enum SymptomType: String, CaseIterable, Codable {
    case palpitations = "Palpitations"
    case anxiety = "Anxiety"
    case dizziness = "Dizziness"
    case fatigue = "Fatigue"
    case shortnessOfBreath = "Shortness of Breath"
    case chestDiscomfort = "Chest Discomfort"
}

@Model
final class SymptomLog {
    var id: UUID
    var symptomType: String
    var timestamp: Date
    var notes: String?
    
    init(symptomType: SymptomType, timestamp: Date = .now, notes: String? = nil) {
        self.id = UUID()
        self.symptomType = symptomType.rawValue
        self.timestamp = timestamp
        self.notes = notes
    }
}
```

Export SymptomType enum and SymptomLog model.
  </action>
  <verify>
    <automated>ls -la AFOne/Models/SymptomLog.swift</automated>
  </verify>
  <done>SymptomLog model created with @Model macro, SymptomType enum with 6 cases</done>
</task>

<task type="auto">
  <name>task 2: Create TriggerLog SwiftData model</name>
  <files>AFOne/Models/TriggerLog.swift</files>
  <action>
Create SwiftData @Model for trigger logging:

```swift
import SwiftData

enum TriggerType: String, CaseIterable, Codable {
    case alcohol = "Alcohol"
    case caffeine = "Caffeine"
    case stress = "Stress"
    case poorSleep = "Poor Sleep"
    case heavyMeals = "Heavy Meals"
    case intenseExercise = "Intense Exercise"
}

@Model
final class TriggerLog {
    var id: UUID
    var triggerType: String
    var timestamp: Date
    var intensity: String?
    var notes: String?
    
    init(triggerType: TriggerType, timestamp: Date = .now, intensity: String? = nil, notes: String? = nil) {
        self.id = UUID()
        self.triggerType = triggerType.rawValue
        self.timestamp = timestamp
        self.intensity = intensity
        self.notes = notes
    }
}
```

Export TriggerType enum and TriggerLog model.
  </action>
  <verify>
    <automated>ls -la AFOne/Models/TriggerLog.swift</automated>
  </verify>
  <done>TriggerLog model created with @Model macro, TriggerType enum with 6 cases, optional intensity field</done>
</task>

<task type="auto">
  <name>task 3: Configure ModelContainer in App entry</name>
  <files>AFOne/AFOneApp.swift</files>
  <action>
Update AFOneApp to include ModelContainer for SwiftData models:

```swift
@main
struct AFOneApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .modelContainer(for: [SymptomLog.self, TriggerLog.self])
    }
}
```

This enables @Query and ModelContext injection throughout the app.
  </action>
  <verify>
    <automated>grep -l "modelContainer" AFOne/AFOneApp.swift</automated>
  </verify>
  <done>App configured with ModelContainer for SymptomLog and TriggerLog</done>
</task>

</tasks>

<verification>
- SymptomLog.swift exists with @Model macro
- TriggerLog.swift exists with @Model macro  
- AFOneApp.swift includes modelContainer(for:)
- Both models export their enums for use in UI
</verification>

<success_criteria>
SwiftData models ready for @Query usage in views. Symptom and trigger data can be persisted locally.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-input-analysis/02-01-SUMMARY.md`
</output>
